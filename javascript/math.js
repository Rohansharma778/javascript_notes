//math object
let x=-45;
let y=2;
let z=1;

 z=Math.round(x);// rounds up the value
 z=Math.floor(x);// round down the value
 z=Math.ceil(x); //round up the value
 z=Math.trunc(x); //eliminate the value after the point
 z=Math.pow(x,2)//or Math.pow(y,x)the answer is 8 calculate the power
 z=Math.sqrt(x); //calculate the square root
 z=Math.log(x); //calculate the log value
 z=Math.sin(x);//trigo calculation cos tan etc
 z=Math.abs(x);//makes the negative number to positve
z=Math.sign(x); //tell the sign of the value in -1 and 1 or 0 if value is 0
// Math.min and Math.max compare the value and give answer according 45 is max and 1 is min

console.log(z);